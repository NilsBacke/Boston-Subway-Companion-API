# Javascript Node CircleCI 2.0 configuration file
#
# Check https://circleci.com/docs/2.0/language-javascript/ for more details
#
version: 2
jobs:
    node:
        docker:
            # specify the version you desire here
            - image: circleci/node:8

            # Specify service dependencies here if necessary
            # CircleCI maintains a library of pre-built images
            # documented at https://circleci.com/docs/2.0/circleci-images/
            # - image: circleci/mongo:3.4.4

        working_directory: ~/repo

        steps:
            - checkout

            # Download and cache dependencies
            - restore_cache:
                  keys:
                      - v1-dependencies-{{ checksum "package.json" }}
                      # fallback to using the latest cache if no exact match is found
                      - v1-dependencies-

            - run: yarn install

            - save_cache:
                  paths:
                      - node_modules
                  key: v1-dependencies-{{ checksum "package.json" }}

            # run type check!
            - run:
                  name: type check
                  command: npm run type-check
    develop:
        docker:
            # specify the version you desire here
            - image: circleci/node:8

        working_directory: ~/repo

        steps:
            - checkout

            - run: sudo apt-get update
            - run: sudo apt-get install zip -y
            - run: sudo apt-get install python3 -y
            - run: sudo apt-get install python3-pip -y
            - run: sudo pip3 install awscli --upgrade
            - run: zip -r ./lambda.zip *
            - run: sudo aws lambda update-function-code --function-name arn:aws:lambda:us-east-1:377533301042:function:lucasCompanies --zip-file fileb://lambda.zip

workflows:
    version: 2
    build:
        jobs:
            - node:
                  context: subway-api-aws
                  filters:
                      branches:
                          ignore: [develop, master]
    deploy-develop:
        jobs:
            - node:
                  context: subway-api-aws
                  filters:
                      branches:
                          only: develop
            - develop:
                  requires:
                      - node
                  context: subway-api-aws
                  filters:
                      branches:
                          only: develop
